#include "main.h"
/**
 * _abs - the function that computes the absolute value of an int
 *@s: the value
 * Description: The standard library provides a similar funct..
 *
 * Return: (0)
 *
 */
int _abs(int s)
{
	if (s >= 0)

		return (s);
	else
		return (s * -1);
}
